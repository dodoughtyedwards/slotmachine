namespace SimplifiedSlotMachine.Configuration
{
    public enum SymbolType
    {
        Wildcard,
        Apple,
        Banana,
        Pineapple,
        // Added Undefined as the explicit value to initialise to before being read from config
        Undefined
    }
}